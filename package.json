{
  "homepage": "https://seder111.github.io/avocados",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "deploy": "npm run build && npx gh-pages -d ./build",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\"",
    "pre-commit": "pretty-quick --staged",
    "install:husky": "husky install"
  },
  "devDependencies": {
    "husky": "7",
    "prettier": "^2.0.5",
    "pretty-quick": "^3.1.0",
    "snowpack": "^3.0.11",
    "tailwindcss": "^2.0.2"
  },
  "dependencies": {
    "gh-pages": "^6.0.0"
  }
}
